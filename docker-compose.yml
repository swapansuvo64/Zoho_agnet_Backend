services:
  redis:
    image: redis:7-alpine
    command: redis-server --maxmemory 256mb
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
